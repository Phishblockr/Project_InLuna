const { mongoose } = require("mongoose");

const getAllLogs = async () => {
  try {
    const logs = mongoose
  } catch (error) {
    console.log(error.message);
  }
}

module.exports = {
  getAllLogs,
}